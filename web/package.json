{
  "name": "ftc-biobuzz-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser MuJoCo WASM viewer for FTC BIOBUZZ sim",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@mujoco/mujoco": "^3.14.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
